# GitHub Pages setup

1. Upload the contents of this folder to the root of a repository.
2. In repository settings, enable GitHub Pages from the main branch and root folder.
3. Open the published `index.html`.

The course is a static website and does not require a build command. Student work remains in each browser unless exported as PDF, JSON or ZIP.
